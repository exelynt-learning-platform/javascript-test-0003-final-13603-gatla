let n = 6;

for (let i = 1; i <= n; i++) {

    let row = "";
    let num = i % 2 === 0 ? 0 : 1;

    for (let j = 1; j <= i; j++) {
        row += num;
        num = num === 0 ? 1 : 0;
    }

    console.log(row);
}
